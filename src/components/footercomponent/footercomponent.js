import './footercomponent.css';

export default function FooterComponent() {
    const cssPrefix = 'footercomponent'
    return (
        <div className={`${cssPrefix}__main_container`}>

        </div>
    )
}